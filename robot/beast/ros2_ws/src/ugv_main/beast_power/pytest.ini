[pytest]
markers =
    integration: real-ROS end-to-end tests (real rclpy + DDS over loopback) in test/integration — excluded from the host (strict-stub) lane with -m "not integration", run in the CI ros:humble-ros-base container with -m integration
